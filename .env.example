# ============================================================
# Tata CLiQ E-Commerce Clone — Environment Variables
# Copy to .env and fill in values. NEVER commit .env to git.
# ============================================================

# ---- SQL Server ----
SQLSERVER_SA_PASSWORD=YourStr0ngP@ssword!
SQLSERVER_HOST=localhost
SQLSERVER_PORT=1433
SQLSERVER_DB=TataCliqDb

# ---- Connection Strings (used in appsettings.Development.json) ----
ConnectionStrings__DefaultConnection=Server=localhost,1433;Database=TataCliqDb;User Id=sa;Password=YourStr0ngP@ssword!;TrustServerCertificate=True

# ---- JWT (RS256) ----
# Generate: openssl genrsa -out private.pem 2048 && openssl rsa -in private.pem -pubout -out public.pem
Jwt__PrivateKeyPath=./keys/private.pem
Jwt__PublicKeyPath=./keys/public.pem
Jwt__Issuer=https://tatacliq-auth.local
Jwt__Audience=tatacliq-spa
Jwt__AccessTokenExpiryMinutes=15
Jwt__RefreshTokenExpiryDays=7

# ---- Redis (V2 — not used until Phase 5) ----
# Redis__ConnectionString=localhost:6379

# ---- Azure (V2 — DO NOT configure until Phase 5) ----
# Azure__BlobStorage__ConnectionString=
# Azure__ServiceBus__ConnectionString=
# Azure__CognitiveSearch__Endpoint=
# Azure__CognitiveSearch__ApiKey=
# Azure__OpenAI__Endpoint=
# Azure__OpenAI__ApiKey=
# Azure__KeyVault__VaultUri=

# ---- Razorpay (V2 — DO NOT configure until Phase 5) ----
# Razorpay__KeyId=
# Razorpay__KeySecret=

# ---- Notification (V2) ----
# Msg91__AuthKey=
# Azure__CommunicationServices__ConnectionString=
# Fcm__ServerKey=

# ---- Angular ----
# Set in frontend/src/environments/environment.ts
ANGULAR_API_BASE_URL=http://localhost:5000
ANGULAR_AUTH_API_URL=http://localhost:5001
ANGULAR_CATALOG_API_URL=http://localhost:5002
ANGULAR_CART_API_URL=http://localhost:5003
ANGULAR_ORDER_API_URL=http://localhost:5004
